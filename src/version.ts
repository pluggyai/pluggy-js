// Project versions
// Needs to be updated manually since in client-side, we don't have access to the package.json file.
export const PLUGGY_JS_VERSION = '0.10.0'
export const AXIOS_VERSION = '^0.21.1'
